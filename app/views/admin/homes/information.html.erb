<div class = "container">
  <div class = "row">
    <h3 class = "title">未完了の注文一覧<small class = "ml-2 mt-2">全<%= @orders.where.not(status: 3).count %>件</small></h3>
  </div>
  <div class = "row mt-3">
    <div class = "col-lg-4">
      <h4>発送準備中一覧<small class = "ml-2"><%= @orders.where(status: 2).count %>件</small></h4>
      <% if @orders.where(status: 2).present? %>
        <table class = "table text-center">
          <tr style="background-color:rgba(213,219,219,0.50);">
            <th>注文日時</th>
            <th>注文者</th>
            <th></th>
          </tr>
          <% @orders.where(status: 2).each do |order| %>
            <tr class = " bg-white">
              <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= order.customer.last_name %><%= order.customer.first_name %><small>さん</small></td>
              <td><%= link_to "詳細へ",admin_order_path(order), class: "btn btn-info btn-sm" %></td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <h5>現在発送準備中の注文はありません。</h5>
      <% end %>
    </div>
    <div class = "col-lg-4">
      <h4>製作中一覧<small class = "ml-2"><%= @orders.where(status: 1).count %>件</small></h4>
      <% if @orders.where(status: 1).present? %>
        <table class = "table text-center">
          <tr style="background-color:rgba(213,219,219,0.50);">
            <th>注文日時</th>
            <th>注文者</th>
            <th></th>
          </tr>
          <% @orders.where(status: 1).each do |order| %>
            <tr class = " bg-white">
              <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= order.customer.last_name %><%= order.customer.first_name %></td>
              <td><%= link_to "詳細へ",admin_order_path(order), class: "btn btn-info btn-sm" %></td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <h5>現在製作中の注文はありません。</h5>
      <% end %>
    </div>
    <div class = "col-lg-4">
      <h4>未入金者一覧<small class = "ml-2"><%= @orders.where(status: 0).count %>件</small></h4>
      <% if @orders.where(status: 0).present? %>
        <table class = "table text-center">
          <tr style="background-color:rgba(213,219,219,0.50);">
            <th>注文日時</th>
            <th>注文者</th>
            <th></th>
          </tr>
          <% @orders.where(status: 0).each do |order| %>
            <tr class = " bg-white">
              <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= order.customer.last_name %><%= order.customer.first_name %></td>
              <td><%= link_to "詳細へ",admin_order_path(order), class: "btn btn-info btn-sm" %></td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <h5>現在入金待ちの注文はありません。</h5>
      <% end %>
    </div>
  </div>
</div>